import styles from './Settings.module.css'

export function Settings() {
  return (
    <section className={styles.settings}>
      <h2>Settings</h2>
      <p className={styles.subtitle}>Settings will be available in a future phase.</p>
    </section>
  )
}

export default Settings
